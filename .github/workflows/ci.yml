# Catches a broken build on a PR before it is merged. Pushes to main/develop
# are built by deploy.yml, so this only runs on pull requests.
name: CI

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      # `next build` prerenders pages, so it needs to reach the Sanity dataset.
      # Add NEXT_PUBLIC_SANITY_PROJECT_ID as a repository *variable*
      # (Settings → Secrets and variables → Actions → Variables) to enable it.
      - run: npm run build
        if: ${{ vars.NEXT_PUBLIC_SANITY_PROJECT_ID != '' }}
        env:
          NEXT_PUBLIC_SANITY_PROJECT_ID: ${{ vars.NEXT_PUBLIC_SANITY_PROJECT_ID }}
          NEXT_PUBLIC_SANITY_DATASET: ${{ vars.NEXT_PUBLIC_SANITY_DATASET || 'production' }}
